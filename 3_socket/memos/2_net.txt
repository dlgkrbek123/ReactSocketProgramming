---------------------------------------------------------------------------------

net 모듈
=> TCP 스트림 기반의 비동기 네트워크 통신을 제공

const server = net.createServer((socket)=> {
  socket.on('data', () => {})
  socket.on('close', () => {})

  socket.write()
})

---------------------------------------------------------------------------------
